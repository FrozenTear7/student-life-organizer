import { ADD_TODO, EDIT_TODO, DELETE_TODO } from '../../constants/index'

const INITIAL_STATE = {
    addedTodos: { todos: [] }
}

const todos = (state = INITIAL_STATE, action) => {
    switch (action.type) {
        case ADD_TODO:
            return {  }
    }
}
